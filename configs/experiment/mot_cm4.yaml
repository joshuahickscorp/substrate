# MoT preregistration mirror (WP-02): registry row mot_cm4_workspace_shell in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_thinking/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through devsys.experiments.REGISTRY: scripts/mot_cm4_workspace_pilot.py
# writes its verdict json to runs/mot/cm4_workspace_pilot.json.
# PILOT: laptop-scale evidence only; the registered claim needs studio-scale
# (see the row's relation field and STUDIO_HANDOFF.md).
id: mot_cm4_workspace_shell
name: workspace_shell_routing
module: scripts/mot_cm4_workspace_pilot.py
metric: [backward_transfer, planning_gain, seed_sign_flips]
null_hypothesis: "the workspace shell ties param-matched dense OR ties matched-compute unrolled depth on BWT and planning, or sign-flips across seeds: the win is capacity/depth, not routing"
falsifier: "ties dense OR unrolled depth, or sign-flips"
tier: cpu-now
seeds: '0-2'
output: runs/mot/cm4_workspace_pilot.json
pilot: true
