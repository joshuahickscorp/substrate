# MoT preregistration mirror (WP-02): registry row mop_at4_programmatic_ceiling in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_perspectives/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through mop.experiments.REGISTRY: scripts/mop_at4_programmatic_ceiling.py
# writes its verdict json to runs/mot/at4_programmatic_ceiling.json.
id: mop_at4_programmatic_ceiling
name: programmatic_ceiling_reference
module: scripts/mop_at4_programmatic_ceiling.py
metric: [programmatic_vs_perceptual_per_factor]
null_hypothesis: "the programmatic reference does not exceed the perceptual substrates on any atlas factor (trivially separable everywhere), so no perceptual tie can be attributed to a substrate bound"
falsifier: "programmatic never exceeds perceptual"
tier: cpu-now
seeds: '0-4'
output: runs/mot/at4_programmatic_ceiling.json
pilot: false
