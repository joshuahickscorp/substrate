# MoT preregistration mirror (WP-02): registry row mot_dr13_horizon_limit in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_thinking/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through devsys.experiments.REGISTRY: scripts/mot_dr13_horizon_limit.py
# writes its verdict json to runs/mot/dr13_horizon_limit.json.
id: mot_dr13_horizon_limit
name: planning_horizon_limit
module: scripts/mot_dr13_horizon_limit.py
metric: [accuracy_vs_horizon_crossover]
null_hypothesis: "planning never beats reactive at any horizon (contradicts ex2) OR beats it only at horizon 1 (not really planning)"
falsifier: "no horizon where planning > reactive, or only H=1"
tier: cpu-now
seeds: '0-4'
output: runs/mot/dr13_horizon_limit.json
pilot: false
