# MoT preregistration mirror (WP-02): registry row mot_al2_shared_latent_alignment in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_thinking/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through devsys.experiments.REGISTRY: scripts/mot_al2_alignment_pilot.py
# writes its verdict json to runs/mot/al2_alignment_pilot.json.
# PILOT: laptop-scale evidence only; the registered claim needs studio-scale
# (see the row's relation field and STUDIO_HANDOFF.md).
id: mot_al2_shared_latent_alignment
name: shared_latent_alignment_vs_random_map_floor
module: scripts/mot_al2_alignment_pilot.py
metric: [prediction_r2_over_random_map_floor]
null_hypothesis: "a random map of equal rank predicts the target as well as the learned map (alignment-artifact): any apparent cross-substrate agreement is the map's capacity, not shared structure"
falsifier: "learned map == random-map-of-equal-rank"
tier: cpu-now
seeds: '0-4'
output: runs/mot/al2_alignment_pilot.json
pilot: true
