# MoT preregistration mirror (WP-02): registry row mop_ws3_arbitration in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_perspectives/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through mop.experiments.REGISTRY: scripts/mop_ws3_arbitration.py
# writes its verdict json to runs/mot/ws3_arbitration.json.
id: mop_ws3_arbitration
name: uncertainty_vs_disagreement_arbitration
module: scripts/mop_ws3_arbitration.py
metric: [speed_accuracy_frontier, auroc]
null_hypothesis: "precision weights are uninformative so inverse-variance ties equal-weight averaging, and disagreement is uncorrelated with correctness so disagreement routing ties random routing at matched rate; OR disagreement fires on aleatoric noise (fails noisy-TV)"
falsifier: "inverse-variance ties averaging AND disagreement ties random routing"
tier: cpu-now
seeds: '0-4'
output: runs/mot/ws3_arbitration.json
pilot: false
