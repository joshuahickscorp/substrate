id: i1_info_bottleneck
name: information_bottleneck_capability_per_bit
module: i_infotheory
metric: [probe_accuracy_vs_retained_bits, knee_width_95pct, max_small_width_separation]
null_hypothesis: "accuracy vs retained bits is flat to a tiny width (redundant) or the real curve ties a frozen-random projection of equal width (generic bottleneck)"
tier: cpu-now

dim: 48
n_classes: 4
samples: 240
separation: 2.0
widths: [1, 2, 4, 8, 16, 32]
sep_margin: 0.1
seeds: [0, 1]
