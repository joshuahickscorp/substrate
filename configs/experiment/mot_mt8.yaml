# MoT preregistration mirror (WP-02): registry row mot_mt8_latent_debate in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_thinking/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through devsys.experiments.REGISTRY: scripts/mot_mt8_latent_debate.py
# writes its verdict json to runs/mot/mt8_latent_debate.json.
id: mot_mt8_latent_debate
name: latent_debate_vs_single_module
module: scripts/mot_mt8_latent_debate.py
metric: [error_at_matched_total_flops]
null_hypothesis: "debate ties the single matched module AND ties a plain ensemble; structured exchange is an unrolled ensemble/MoE"
falsifier: "debate <= max(single, ensemble)"
tier: cpu-now
seeds: '0-4'
output: runs/mot/mt8_latent_debate.json
pilot: false
