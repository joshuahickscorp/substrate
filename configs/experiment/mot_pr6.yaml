# MoT preregistration mirror (WP-02): registry row mot_pr6_sleep_consolidation in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_thinking/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through devsys.experiments.REGISTRY: scripts/mot_pr6_sleep_consolidation.py
# writes its verdict json to runs/mot/pr6_sleep_consolidation.json.
id: mot_pr6_sleep_consolidation
name: offline_sleep_consolidation
module: scripts/mot_pr6_sleep_consolidation.py
metric: [backward_transfer, frontier_auc]
null_hypothesis: "offline replay-only ties online replay at matched total gradient steps: any sleep benefit is added compute, not phase separation"
falsifier: "sleep delta <= 0 at matched steps"
tier: cpu-now
seeds: '0-4'
output: runs/mot/pr6_sleep_consolidation.json
pilot: false
