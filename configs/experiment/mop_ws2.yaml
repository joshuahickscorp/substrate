# MoT preregistration mirror (WP-02): registry row mop_ws2_fusion_tournament in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_perspectives/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through mop.experiments.REGISTRY: scripts/mop_ws2_fusion_tournament.py
# writes its verdict json to runs/mot/ws2_fusion_tournament.json.
id: mop_ws2_fusion_tournament
name: matched_capacity_fusion_tournament
module: scripts/mop_ws2_fusion_tournament.py
metric: [held_out_accuracy, nll_at_matched_capacity]
null_hypothesis: "at matched capacity every structured fusion ties the concat-MLP on held-out accuracy and NLL: structure buys nothing beyond param count"
falsifier: "every structured arm ties concat-MLP"
tier: cpu-now
seeds: '0-4'
output: runs/mot/ws2_fusion_tournament.json
pilot: false
