id: s3_concept_arithmetic
name: latent_concept_arithmetic
module: s_semiotics
metric: [retrieval_at1, gain_vs_random_offset, gain_vs_frozen_random, probe_r2]
null_hypothesis: "offset arithmetic ties a random matched-norm offset, OR it survives equally under a frozen-random projection (a generic property of any linear space, not of V-JEPA concepts)"
tier: cpu-now

dim: 48
n_factor: 4
per_cell: 24
separation: 1.5
seeds: [0, 1]
