# MoT preregistration mirror (WP-02): registry row mot_dr14_corruption in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_thinking/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through devsys.experiments.REGISTRY: scripts/mot_dr14_corruption.py
# writes its verdict json to runs/mot/dr14_corruption.json.
id: mot_dr14_corruption
name: reasoning_under_corruption_compression
module: scripts/mot_dr14_corruption.py
metric: [accuracy_vs_corruption_slope]
null_hypothesis: "reasoning and single-pass degrade at the same rate under every corruption: iteration only processes what survives"
falsifier: "equal slopes"
tier: cpu-now
seeds: '0-2'
output: runs/mot/dr14_corruption.json
pilot: false
