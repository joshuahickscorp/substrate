# MoT preregistration mirror (WP-02): registry row mop_at5_probe_class_sweep in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_perspectives/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through mop.experiments.REGISTRY: scripts/mop_at5_probe_class_sweep.py
# writes its verdict json to runs/mot/at5_probe_class_sweep.json.
id: mop_at5_probe_class_sweep
name: probe_class_sweep
module: scripts/mop_at5_probe_class_sweep.py
metric: [per_cell_verdict_across_probe_classes]
null_hypothesis: "every cell's verdict is invariant to probe class, so probe choice never changes decodability and probe-specific is an empty verdict"
falsifier: "verdicts invariant to probe class"
tier: cpu-now
seeds: '0-4'
output: runs/mot/at5_probe_class_sweep.json
pilot: false
