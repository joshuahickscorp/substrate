# MoT preregistration mirror (WP-02): registry row mop_pr2_plasticity_substrates in registry/experiments.yaml.
# The null_hypothesis is carried VERBATIM from the registry row (which mirrors section 11 of
# docs/mixture_of_perspectives/11_experiment_registry.md). This row runs as a standalone script
# (status registry-only), not through mop.experiments.REGISTRY: scripts/mop_pr2_plasticity_substrates.py
# writes its verdict json to runs/mot/pr2_plasticity_substrates.json.
id: mop_pr2_plasticity_substrates
name: plasticity_advantage_real_vs_random_encoder
module: scripts/mop_pr2_plasticity_substrates.py
metric: [adaptation_speed, backward_transfer]
null_hypothesis: "shell adaptation speed and BWT on real V-JEPA are within the seed spread of the same shell on random-init-ViT (matched resolution): the substrate's structure does not ease trained-shell learning dynamics"
falsifier: "real-vs-random-encoder adaptation/BWT delta < seed spread"
tier: cpu-now
seeds: '0-4'
output: runs/mot/pr2_plasticity_substrates.json
pilot: false
