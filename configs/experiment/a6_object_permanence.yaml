id: a6_object_permanence
name: object_permanence_persistence
module: a_perception
metric: [mid_occlusion_separation, persistence_vs_frame_only, real_vs_frozen_random]
null_hypothesis: "mid-occlusion the latent carries no decodable trace of the hidden object beyond the empty frame (taxonomy 3), OR a single-frame baseline already separates the classes"
tier: cpu-now

dim: 48
samples: 220
permanence_leak: 0.15
seeds: [0, 1]
