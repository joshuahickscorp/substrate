# Completion Claim Audit, 2026-07-10

## Verdict

The repository is not at a Studio boundary. It contains a large amount of working local
infrastructure, many verified mechanics, several useful pilots, one strongly bound custom-substrate
null, and a small set of tightly scoped F-series scientific results. It also contains unfinished
local science, most immediately the admission-held P5 campaign, progressive P6 stream ladder, and
natural-data work required by E6 and DR14. The five-seed P4 pilot is complete and governor-closed.

No current experiment proves a hardware requirement. The current requirements matrix has zero
category-8 rows, zero category-9 rows, and zero hardware-required rows. The regenerated F boundary
receipt now correctly refuses a Studio handoff because the scientific ledger is not ready, no
measured Studio boundary exists, and non-hardware blockers remain.

The main semantic correction is simple:

> A checked implementation, class smoke, import test, memory probe, cache build, or finite metric is
> not a completed scientific study.

The machine-readable audit is `proof/COMPLETION_CLAIM_AUDIT.json`. It accounts for all 199 `[x]`
checkboxes exactly once by source and line number. Evidence produced after that inventory is stored
in a separate post-inventory layer and contributes zero new checked claims.

## Scope and method

The checkbox surface is frozen to the original audited snapshot:

| Source | Checked claims |
|---|---:|
| `STATUS.md` | 162 |
| `docs/mixture_of_perspectives/STUDIO_TURNKEY_PLAN.md` | 33 |
| `docs/mixture_of_perspectives/STUDIO_RUN_REPORT.md` | 4 |
| Total | 199 |

The audit used the following evidence order:

1. Current raw and composite receipts.
2. Current registry contracts.
3. Current generated ledgers and their embedded source hashes.
4. Durable historical receipts.
5. Narrative status only when no stronger artifact exists.

Every checked claim received a primary evidence class. Composite claims were then decomposed into
their atomic results. The primary checkbox counts are not experiment counts. Later implementation,
mechanics, retirement, or queue evidence updates current scheduling but cannot change this 199-item
denominator or silently upgrade an original checkbox.

| Primary class | Checked claims | What it means |
|---|---:|---|
| Scientific completion | 1 | One composite F-chain checkbox; atomic results are split below. |
| Confirmatory result | 3 | Focused multi-seed closure controls with narrower binding than the current F and CM7 chains. |
| Pilot | 16 | Directional evidence that cannot promote. |
| Mechanics only | 29 | Availability, integrity, execution, smoke, accounting, resource, or ledger proof. |
| Scaffold | 110 | Code, config, registry, orchestration, preregistration, or documentation exists. |
| Stale or unbound | 29 | Historical counts, superseded conclusions, missing raw binding, or wording contradicted by newer evidence. |
| Failed or null | 10 | A tie, refutation, failed verifier, or retired exact regime. |
| Data or rights blocked | 1 | A composite frontier reclassification claim. |
| Implementation blocked | 0 | No checked claim itself belongs here; two current registry rows do. |
| Measured hardware blocked | 0 | No qualifying claim exists. |

## What is scientifically complete

### F-series positive results

Eleven F-series positives meet the current local scientific-completion contract at their exact
structured-synthetic or local scope:

- `f1_form_alignment_gate`
- `f2_heldout_form_transfer`
- `f3_form_bottleneck_capacity`
- `f4_raw_payload_vs_form_tokens`
- `f5_cross_form_memory_binding`
- `f6_sensorimotor_form_closure`
- `f9_cross_form_compositional_binding`
- `f12_private_form_language_stability`
- `f13_form_energy_budget`
- `f15_embodied_affordance_form`
- `f18_counterfactual_form_intervention`

Each has a canonical five-seed receipt, a separate five-fresh-seed verifier, a strict claim gate,
and an exact current contract freeze. I recomputed the live contract bindings for all twelve
candidate-positive verifier files. For the eleven passing rows, the module, class source, config,
registry-row, contract, and verifier hashes all match the current tree. The verifier receipts are
independent and adversarial.

This is real scientific completion only for the receipt scope. It does not establish natural-video,
open-environment, or broad cross-domain behavior.

### F-series negative and blocked results

Six F-series legs are complete local nulls or ties:

- `f7_developmental_form_growth`
- `f11_form_dream_replay`
- `f14_lifelong_form_expansion`
- `f17_missing_form_recovery`
- `f19_cross_scale_referent_binding`
- `f20_substrate_crisis_test`

`f10_intrinsic_form_curriculum` is not a positive. Its fresh verifier fails because the confidence
interval lower bound is not positive and the effect changes sign. The gate correctly refuses the
claim. Evidence:

- `proof/FORM_SUBSTRATE/VERIFIERS/f10_intrinsic_form_curriculum.json`, SHA-256
  `717da79e2d7145112fdfc32a11e474cf42a25935ff4300cbdefebfda55aa40e8`
- `proof/FORM_SUBSTRATE/VERDICT_GATES/f10_intrinsic_form_curriculum.json`, SHA-256
  `b713495b393bb90c9b72d4af1cadc70b58e4ea9f63fa78ebe1f42e6effcad4aa`

`f8_plastic_substrate_rewrite` and `f16_perfect_slate_null` are not completed studies. They have
fail-closed fixture mechanics. Their first blockers are rights-clean training data, trusted
provenance, and executable real-weight input receipts. They are environment work, not GPU work.

## Strong completed null: CM7

CM7 is a well-bound negative result, not a positive lever.

The five-seed local campaign completed at 1.646 million trainable parameters, 1,000 updates,
256-pixel deterministic programmatic video, matched compute, matched random-target, and exact frozen
initialization controls. The independent familywise-corrected verifier reports `not-promoted`. No
learned objective clears the random-target control, frozen initialization, and strongest alternative
objective.

The composite receipt is:

- `proof/CUSTOM_SUBSTRATE_PILOT.json`, SHA-256
  `bcd1fe9f1eacdf4fd7fc3b2d75aad01a799a7b0912c8d1d2553607c7b66387a2`

All four composite chain hashes match their durable copies under
`proof/CUSTOM_SUBSTRATE_PILOT_CHAIN`:

- raw training receipt
- current-evidence attestation
- environment receipt
- independent verifier

The exact regime is retired. Its platform contracts remain reusable. A successor must change a
scientific premise, task, objective, or variance design. More compute alone does not reverse this
null.

## Confirmatory historical controls

Three focused closure experiments resolve important synthetic contentions:

| Result | Exact receipt | Audit interpretation |
|---|---|---|
| Sparse heads reduce forgetting under both original and frozen-random geometry. | `runs/pre_studio/close_e7_sparse.json`, `3a8fd4b27042c6748eb3d8ef8c737474e8daff030a27c10f1dca27e8d613cc7e` | Generic shell architecture effect, not substrate-specific. |
| Matched effective-step plain SGD ties or beats the local rules. | `runs/pre_studio/close_ex5_local_rules.json`, `2205b8bb17ce6a6aa3201f5649ceafe0e8ab71f9c3c6d8348d9986abc4405c67` | The retention claim is refuted as an Adam comparison artifact. |
| MPC beats flat and action-shuffled controls on true synthetic dynamics for three seeds. | `runs/pre_studio/close_ex2_planning.json`, `d884e7a8ada600bf0845fdbb115f2e698f65d5d826aca947b5506cdc9b037acd` | A real synthetic planning result, not natural-environment evidence. |

`proof/ARTIFACT_INDEX/pre_studio.json`, SHA-256
`309d5bffead257e6467f356454602e36fe6a3f9ff47d3b87c67c577604d46aca`, proves the exact durable
bytes. The raw closure JSON files do not embed source, config, manifest, or git hashes. Git history
shows the scripts remain tracked, but that is weaker than the current F and CM7 receipt chains.
These results are therefore confirmatory and decision-useful, but not current-standard project-wide
scientific completions. Rerun them through the bound path only if they become architecture-critical.

## Retired inherited-scale pilot

The original P3 inventory row remains a pilot with complete mechanics and no promotable scientific
claim. Nine matched random-init controls and exact input identity were verified over eight generated
referents. The immutable pilot and identity receipts retain their original model names, hashes, and
commands as historical evidence.

The pilot is now retired from active selectors and from current frontier and requirements reasoning.
Its result remains non-promotable because the inputs are generated, the independent-unit count is
eight, and the held-out cells are weak. Retirement does not delete or reinterpret the historical
receipt, and exact identity never promoted the pilot.

## Post-inventory evidence, not new checked claims

The following work landed after the 199-checkbox inventory. It updates current evidence and queue
state while contributing zero new checked claims.

### Wave E0 event and lifecycle mechanics

`proof/EXPANSION_WAVE0.json`, SHA-256
`653f6f3da22da1dc0cf446217e9726f81c23a59f9193556bb881f8b1052f97b9`, is a mechanics pass for F23,
F29, and F39 over three deterministic programmatic units. The independent verifier recomputes 72
boolean metrics, verifies nine declared controls, and rejects all nine semantic mutations. Its
claim scope is mechanics only, with no capability or natural-data claim.

### P6 continual-stream mechanics and progressive ladder

`proof/CONTINUAL_MILLION_EVENT_PREFLIGHT.json`, SHA-256
`fa1f65a6e839f0e4ac8c310da02f918a5666f70221f012dfb407eb4335d34e03`, verifies a deterministic
disk-backed 384-event stream for abrupt and gradual schedules across replay, no-replay, and
fresh-init controls. Cursor resume, exact identity, deletion, matched updates, and metric-family
plumbing pass. This is mechanics only.

The declared execution ladder is 10,000 events, then 100,000 events, then a conditional one-million
event rung. The one-million rung is earned only after the two smaller rungs pass atomic recovery,
independent metric replay, resource admission, and useful-retention controls across at least five
independent seeds. No hardware boundary or continual-learning result is established by 384 events.

### P7 action-world-model fixture mechanics

`proof/P7_ACTION_WORLD_MODEL_PREFLIGHT.json`, SHA-256
`5f69a3f6bf8e68d0710e2d180288109068ab1adca5cb273edb5105a36b6b1ca9`, binds deterministic core
SHA-256 `e3d2f9bdb9ee88aa10a67cdcaea2090e2a9cb7c02ce3f54170f9810c60d114ec`. Three deterministic
rendered-fixture units pass replay, same-parent intervention, eight-arm, equal-core-compute,
calibration, horizon, planning, cost, and mutation mechanics. The fixture supports the declared
null: neither primary transition arm beats both its prediction and planning controls. The rerun used
CPU only, took 7.696731 seconds, and reached 302,596,096 bytes sampled peak RSS, with a 91,471,872-byte
phase-local increment. A fail-closed import
guard recorded no inherited-model import attempt and no forbidden source import. This is
mechanics only, with an evidence ceiling of R1 programmatic and R0 external; it is not a scientific,
natural-data, embodiment, cognition, or capability result.

### P9 causal-monitoring fixture mechanics

`proof/P9_CAUSAL_MONITORING_PREFLIGHT.json`, SHA-256
`bb159720a641acb0b8f66503591e4ea879a761787cfe35a5aff94d99a91c9797`, binds deterministic core
SHA-256 `e6fdd287a01edd87771dcbd67746b9e83fd5686c2984e5b835257b86dbe7dffa`. Five deterministic units
cover 260 disjoint lineages and 1,300 same-parent branches; exact interrupted resume passes and all
eight semantic mutations are rejected. Brier, ROC-AUC, intervention-sign, and controller-utility
deltas are positive over the correlational fixture control in all five units. These are mechanics
only at R1 programmatic and R0 natural-external, not a promoted result. Energy is unmeasured, and
independent natural workloads, physical-failure episodes, real interventions, replicated
calibration, and a metered boundary remain absent. A fail-closed import guard recorded no
inherited-model import attempt and no forbidden source import. `scientific_claim_ready` is false.

### Governed rewrite fixture mechanics

`proof/GOVERNED_REWRITE_PREFLIGHT.json`, SHA-256
`d94e046b0a3ae7843f490a41e992145e0e79405f2c9ace5ab49bc8a29b715df0`, binds deterministic core
SHA-256 `d2a93c28edd5ceed3f0db7d254ec0dfc26c406e20996309fe7648673f8441b94`. A scoped single-use
fixture transaction improves canary accuracy from 7/8 to 8/8 and shadow accuracy from 9/12 to
12/12 with zero protected regressions. Exact interrupt/resume and atomic commit pass; an authorized
regressing proposal rolls back; all eight adversarial transactions fail closed; and all 14 semantic
mutations are rejected. The CPU-only run took 0.022590 seconds and reached 204,423,168 bytes
sampled peak RSS, with a 425,984-byte phase-local increment. This is mechanics only at R1 fixture
and R0 production. External authority, independent
evaluator ownership, natural workloads, real rewrite artifacts, concurrent and power-loss
durability, and replicated surveillance remain absent. `scientific_claim_ready` is false.

### E6 and DR14 integration complete, science still blocked

`proof/E6_VITB_DENSE_PREFLIGHT.json`, SHA-256
`efa875cf01b9da0bd546b9860576c5488b092bae9facb8943373b4d5d40f52af`, verifies the official dense
ViT-B runtime authority, cache/control interfaces, E6 registration, and DR14 deterministic nested
dropped-channel path without launching another heavy model. Implementation is ready. Both scientific
gates remain false because the rights-clean annotated natural cohort, untouched test membership,
serial learned/random caches, and independent statistical verifiers are absent.

### Current project exhaustion and selector retirement

The refreshed 177-row project ledger contains 120 freshly executed codepaths, 37 durable artifacts,
1 implementation blocker, 11 rights/data blockers, 8 upstream/model blockers, and 0 measured
hardware blockers. Every row still has `scientific_claim_ready: false`.

Inherited larger-scale configs and generators are archived outside the live encoder group. Current
selectors, frontier classification, and requirements planning no longer consume the retired pilot.
The immutable receipts and local cache bytes remain historical evidence.

## P4 is complete as a programmatic pilot

The five-seed campaign closed all 12 registered cells. Six cells stopped by the preregistered
futility rule and the response-surface fit contains 48 observations. The canonical proof and run
receipt are byte-identical:

- `proof/P4_CAPABILITY_DENSITY_SCREEN.json`, SHA-256
  `6330950bfab8d34a9864dd6a98df9b6a69c4a17085ed64a4cecf58669ef58da0`
- `runs/p4_screen/p4screen/p4_screen_receipt.json`, SHA-256
  `6330950bfab8d34a9864dd6a98df9b6a69c4a17085ed64a4cecf58669ef58da0`
- `proof/LOCAL_THROTTLE_P4_RUN.json`, SHA-256
  `709e077bc0665176585f656a1a5d1eb654ccfe5e588fc012cb342ad7aa4c299a`

The governor closed with no active lane. The result remains a programmatic pilot and refuses
confirmatory promotion by construction. Its embedded throttle policy hash is the immutable
execution-time policy snapshot, not a claim that it equals the current scheduling policy.

## P5 is implemented and queued, not run as a full study

P5 has code, configs, registration, preregistration, a bounded class smoke, and a cold-process memory
trace. The memory trace is mechanics only:

- `proof/P5_MEMORY_BOUNDARY_TRACE.json`, SHA-256
  `78db1cfa4ac6343484e3eeefd95a2461328d2396d3fb5b6a7a88fb25fc4d6b24`

The full pilot artifact `proof/P5_CAPABILITY_CONTEXT_SCREEN.json` does not exist. P4 released the
one-heavy-lane governor, but the first P5 execute attempt failed closed before its child command ran.
The governed execute receipt recorded three available-memory samples of 8.044642304, 8.044658688,
and 8.006565888 GB against the 10.0 GB admission threshold. Its first blocker is
`transient-current-host-memory-headroom`, not hardware necessity. The current dry preflight also
denies all three samples and is bound at `proof/LOCAL_THROTTLE_P5_SMOKE_PREFLIGHT.json`, SHA-256
`f3a06974e73a07ddf86347f95d79b630eee4782788a495d1e8313925cd4af794`.

The correct order after three healthy P5 admission samples is:

1. `p5smoke`
2. cold-process training-grid memory pass
3. `p5pilot` seed-0 and off-ceiling staging
4. remaining eligible seeds

## Why the exhaustion ledger does not close the science

`proof/PROJECT_EXPERIMENT_EXHAUSTION.json`, SHA-256
`4bf8830aaf02b9a15efa1b7b7f938f672bf3769e47f80fecef4320082d570317`, accounts for 177 non-F
rows:

- 120 freshly executed and verified codepaths
- 37 durable hash-verifiable artifacts
- 1 implementation-blocked row
- 11 rights or data-blocked rows
- 8 upstream or citable-input-blocked rows
- 0 runnable-not-yet-run rows
- 0 measured-hardware-blocked rows

Every one of the 177 entries has `scientific_claim_ready: false`.

The ledger is internally honest about this, but the phrase `runnable-not-yet-run: 0` is easy to
misread. It means every eligible experiment class or standalone artifact was exercised or accounted
for. It does not mean every full registered study was run. P4 and P5 demonstrate the distinction.

A future ledger should separate:

1. class codepath executed;
2. mechanics receipt complete;
3. pilot complete;
4. registered seed, sample, and control plan complete;
5. independent verifier complete;
6. scientific claim ready.

## Current blockers, correctly named

### Implementation

One non-F row is implementation blocked:

- `e10_openended`: persistent action mechanics exist, but bounded population search, environment
  generation, sustained-horizon evaluation, and transfer gates do not.

### Rights and data

Eleven rows are rights or data blocked:

- `e6_relational`
- `mop_dr1_video_cache`
- `mop_dr3_latent_scratchpad`
- `mop_dr4_causal_intervention`
- `mop_dr7_latent_cot`
- `mop_dr14_corruption`
- `mop_al2_shared_latent_alignment`
- `mop_al3_audio_video_alignment`
- `mop_cm1_compositional_gate`
- `mop_cm3_dense_vs_pooled`
- `mop_cm9_slot_jepa_binding`

### Upstream or citable inputs

Eight rows are grouped under the exhaustion ledger's upstream-input class:

- `mop_dr2_sparse_real`
- `mop_dr5_cross_substrate_consistency`
- `mop_dr15_modality_general`
- `mop_at1_nuisance_grid`
- `mop_cm2_atlas_gate`
- `mop_cm4_workspace_shell`
- `mop_cm6_distilled_density`
- `mop_cm8_custom_jepa_pilot`

This bucket is too coarse for scheduling. Several rows no longer await an unavailable model. They
await a locally buildable cache, an expanded citable task, or a successor scientific dependency.
In particular:

- E6 and DR14 already have official ViT-B runtime, cache/control integration, and fail-closed
  registered consumers. Their next blocker is a rights-clean annotated natural cohort, an untouched
  test, serial learned/random cache materialization, and independent verification. Exact runtime receipts are
  `proof/VJEPA21_VITB_LOAD.json` at
  `c60eb456726e02773c30f42af093727b600e5a6eb67d5d3d094adb17cee241da`,
  `proof/VJEPA21_VITB_FORWARD.json` at
  `06ed0d097a50b2638d0209b991c0710574a750edb4a5ddfb3a5dace722581601`, and
  `proof/VJEPA21_VITB_FORWARD_64F.json` at
  `902422b7c042969076bccbbcf283a9b1b80639a9d9e4a9e24c2ca703fcd5c6bf`. The task integration receipt
  is `proof/E6_VITB_DENSE_PREFLIGHT.json` at
  `efa875cf01b9da0bd546b9860576c5488b092bae9facb8943373b4d5d40f52af`.
- DR5 and AL2 need a meaningful shared task, enough same-task rows, equal-rank mappings, and matched
  controls. The retired inherited-scale pilot is historical availability evidence, not a current
  scientific dependency.
- CM8 cannot keep saying it awaits positive CM7 evidence. CM7 is complete and negative. CM8 needs a
  successor design plus natural same-referent teacher inputs.

### Hardware

There are no measured hardware-blocked rows.

## Discrepancies found

### Found and resolved during the audit

The prior `proof/FORM_SUBSTRATE/PRE_STUDIO_BOUNDARY.json` set
`studio_is_only_remaining_hardware_boundary: true` even though it had no verified Studio boundaries
and listed only non-hardware blockers. The generator was repaired and the receipt regenerated.

Current receipt:

- `proof/FORM_SUBSTRATE/PRE_STUDIO_BOUNDARY.json`, SHA-256
  `b9ae786ad93f74ee58e4f389ac1ad84ffea64801997832c6dfe35a5c64a9e11e`
- `studio_is_only_remaining_hardware_boundary: false`
- `ready_for_studio_handoff: false`
- refusal reasons: scientific ledger not ready, no measured Studio boundary, and non-hardware
  blockers remain

The frontier and requirements receipts are regenerated and current:

1. `proof/FRONTIER_LOCALIZATION.json` consumes the complete five-seed CM7 null, the E6/DR14 task
   integration receipt, and no longer uses the retired inherited-scale pilot to classify AL2 or
   DR5. CM8 correctly says the positive CM7 prerequisite failed. SHA-256:
   `465974192469f12b1345e6fe6005ef2abf374319dc31d92aea2d67308df65c84`.
2. `proof/EXTENDED_COMPUTE_REQUIREMENTS.json` treats E6 and DR14 implementation as complete,
   classifies their first blockers as natural data/cache/verification, and carries no active
   retired-scale requirement. File SHA-256:
   `d3c76b7218b65de0ae54693c1cea7115e7fd28a44717b2fd1dd44329671238ff`.
   Payload SHA-256: `df5d0e4c976004db197c6f14ba9b3ccdf4e1a0dc1f06050895e54af899a4c0ca`.
3. The immutable inherited-scale pilot still binds all nine random controls to exact stimulus
   identity. It is now historical evidence only and is absent from the authoritative current
   evidence list.

The final provenance audit also found that the f36 relation metadata described composition of an
unlanded `recurrent_broadcast` preflight while the executed toy bed used the separate
`integration_broadcast_runs` fixture. The canonical registry now carries an explicitly dated
post-hoc implementation-path correction. Every registry-bound receipt and independent verifier was
regenerated. The result remains mechanics-only.

### Still open

1. Historical prose calls one inherited encoder's MPS failure Studio-necessary. Current CPU and
   official dense execution show that MPS is a poor path on this host, not a necessity boundary.
2. The approximately 821-second MPS figure has no retained raw machine receipt. The current
   `runs/mot/encode_device.json` records a two-clip CPU result and says MPS was skipped.
3. `STATUS.md:579` marks a queued 432-clip build as `[x]`. Scheduling is not completion.
4. Historical test counts, registry counts, stage counts, and cost estimates describe older commits.
   They can remain as chronology but must not serve as current proof.
The retired inherited-scale forward receipts remain immutable history and are excluded from
authoritative current evidence. They are not rerun targets.

## Rerun and integration priority

1. Execute the P5 pilot sequence after three healthy governor samples.
2. Run P6 at 10,000 events, then 100,000; admit one million only if the smaller rungs earn it.
3. Acquire and freeze the rights-clean natural cohort, learned/random caches, and independent
   verifiers required by E6 and DR14.
4. Implement the bounded `e10_openended` population and sustained-horizon gates.
5. Design a preregistered successor to the failed CM7 objective regime before attempting CM8.
6. Rerun the historical e7, ex5, and ex2 closures through current binding only if they will decide
   architecture.

## Final boundary statement

The local machine has disproved several old Studio assumptions. Official dense execution,
custom-substrate training, event/lifecycle mechanics, continual-stream mechanics, and bounded
dense-context mechanics all run locally. Retired inherited-scale artifacts remain history rather
than active selectors. The remaining work is dominated by incomplete local campaigns, progressive
execution, implementation, natural-data intake, rights, and citable cache construction.

A Studio would make some work faster. Current evidence does not show that it enables an experiment
that local factorization cannot estimate. Procurement is therefore not evidence-backed at this
snapshot.
