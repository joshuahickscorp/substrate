# SUBSTRATE COGNITIVE MATERIAL GENESIS II

## Associative plastic fields, compositional closure, binding capability-density frontiers, and the final cognitive-material architecture before tangible sandbox evaluation

---

# 0. AUTHORITY

Operate from:

```text
/Users/scammermike/Downloads/substrate
```

Resolve the actual repository, branches, tags, evidence, running processes, and terminal state before acting.

The latest campaign may still be completing diagnostics or publication. Do not assume the status copied into this document is terminal. Read the authoritative artifacts and adopt the final published values.

Preserve all historical evidence, including every null, positive, transition, limitation, Grok review, mutation result, continuity receipt, and clean-clone record.

The current evidence reported during Cognitive Material Genesis includes:

```text
principal candidate-versus-S2 effect:
    approximately -0.2478

principal 95% CI:
    approximately [-0.2567, -0.2384]

replication and second replication:
    same direction and similar magnitude

hidden composition:
    negative

primary claims passing:
    P1, P2, P3, P5, P6, P7, P8, P9

primary claims failing:
    P4 capability-resource precision value
    P10 decisive architectural advantage
```

Additional reported findings:

```text
verified rewrites improved future behaviour

reversing rewrites removed benefit

topology development beat record storage

different histories produced useful field geometry

shadow fields worked

compiled procedures reduced cost

learning survived interruption and migration

multimodal events updated one shared field

attempt-cap expansion did not rescue candidates

S2 performed thousands of cheap associative writes

field candidates proposed fewer, coarser durable rewrites

absolute memory envelopes did not bind

K10 had seven of nine inert mechanisms

all declared mutations were eventually injected and rejected

clean-clone reproduction passed

continuity survived interruptions and migration
```

Treat these as orientation until verified from terminal evidence.

The new program must not rerun the same tournament at larger scale.

It must solve or decisively close the identified scientific problem:

> Individual field mechanisms work, but the integrated material does not convert them into an advantage over cheap exact associative learning.

External activation remains `false`.

No outcome permits an unqualified Nous declaration.

---

# 1. CORE INTERPRETATION

The prior campaign did not show that endogenous plasticity is useless.

It showed:

```text
mechanism-level value:
    real

composition-level advantage:
    absent

precision-allocation advantage:
    absent or improperly instrumented

integration:
    substantially inert

strongest competing principle:
    cheap fine-grained exact association
```

The next program must therefore distinguish four questions:

```text
1. representation:
    Did S2 win because exact associative storage is superior to the candidate's low-bit representation?

2. update granularity:
    Did S2 win because it can attempt thousands of cheap writes while the field performs coarse structural rewrites?

3. architecture:
    Does a plastic field add value after exact association and fair write granularity are available to both sides?

4. composition:
    Can plasticity, topology, precision, compilation, memory, and shadow cognition cooperate causally rather than coexist decoratively?
```

Do not conflate these.

The field is allowed to incorporate exact associative cognition.

A cognitive material does not become invalid because one of its layers is exact, content-addressable, or monolithic.

---

# 2. TERMINAL OBJECTIVE

Produce one of three outcomes.

## Outcome A — Compositional cognitive material candidate

```text
classification:
    associative_plastic_cognitive_field_candidate

readiness:
    tangible_sandbox_ready
```

Requires a positive, replicated, hidden-composition advantage over the strongest equally resourced and equally plastic alternative.

## Outcome B — Best cognitive material frozen

```text
classification:
    cognitive_material_genesis_ii_complete

status:
    compositional_advantage_unproven

readiness:
    tangible_sandbox_ready
```

Use when the architecture is improved, simplified, and operationally ready, but the decisive advantage remains null.

## Outcome C — Terminal material null

```text
classification:
    terminal_compositional_material_null
```

Use when a critical mechanism, instrument, or architecture cannot be made valid under the bounded candidate program.

No outcome may erase the earlier negative result.

---

# 3. NEW ARCHITECTURAL FAMILY

Build an:

# Associative Plastic Cognitive Field

The field must combine three scales of learning.

## 3.1 Micro-association

Cheap, high-frequency, fine-grained writes:

```text
entity-value association
cue-outcome association
source-reliability update
object identity update
local prediction error
temporary routing preference
```

These writes may be exact, hashed, low-bit, or mixed precision.

They must approach S2's useful write granularity and throughput.

## 3.2 Meso-structure

Verified associations consolidate into:

```text
concept relations
causal pathways
procedures
object-event structures
model competence
goal dependencies
cross-modal bindings
```

Meso-structure should reduce repeated lookup and improve transfer.

## 3.3 Macro-topology

Rare changes:

```text
allocate region
split concept
merge redundancy
compile procedure family
create specialist pathway
prune obsolete organization
```

Macro-topology must not be used for ordinary associative learning.

The previous field likely paid macro-level costs for micro-level problems.

The new material must choose the cheapest sufficient rewrite.

---

# 4. REQUIRED LAYERS

Every candidate may organize these layers differently but must expose equivalent contracts.

## Exact constitutional shell

Owns identity, lineage, evidence, commitments, permissions, activation state, and checkpoint integrity.

## Associative microstore

Provides high-throughput content-addressable learning.

Requirements:

```text
exact and low-bit modes
bounded collision detection
provenance pointers
scope
expiry
confidence
write cost
rollback
```

## Plastic field

Holds persistent developed relations, activations, uncertainty, and learned dynamics.

## Structural consolidator

Converts useful repeated associations into causal, semantic, procedural, and topological structure.

## Shadow field

Supports temporary counterfactual and planning branches.

## Cognitive compiler

Converts stable reasoning traces into monitored procedures.

## Developmental archive

Preserves exact events and enables rollback without loading the entire history.

## Model and body fabric

Tracks replaceable sensors, models, tools, body capabilities, and competence.

---

# 5. REPRESENTATION–ARCHITECTURE FACTORIAL

Before building more candidates, finish or reproduce the pending diagnostic factorial.

At minimum compare:

```text
A:
    S2 exact associative

B:
    S2 constrained low-bit

C:
    field with its original low-bit representation

D:
    field with exact associative microstore

E:
    hybrid field with exact microstore and low-bit structural layer

F:
    equally plastic monolithic hybrid
```

Use equal:

```text
observations
history
write opportunities
compute
memory accounting
tool access
teaching
```

Interpretation:

```text
if low-bit S2 collapses:
    representation explains substantial advantage

if exact field closes the gap:
    write representation/granularity explains substantial advantage

if hybrid monolith still wins:
    field organization remains unsupported

if field wins only after consolidation:
    structural compression may be the missing value

if all tie:
    prefer the simplest material
```

Publish this interpretation before the larger tournament.

---

# 6. UPDATE ECONOMY

Attempt count alone is not fair because update units differ.

Create a common update ledger recording:

```text
proposal
bytes read
bytes written
information introduced
compute
latency
scope
durability
rollback cost
future utility
```

Report:

```text
attempt count
committed count
useful commits
utility per commit
utility per written byte
utility per compute unit
utility per wall-time
```

The field should support:

```text
cheap micro-write
local low-bit adjustment
association promotion
structural consolidation
topology revision
```

A mechanism must not use topology revision where a one-byte association is sufficient.

Train or design a rewrite allocator that selects update granularity before outcome.

Compare against:

```text
always micro
always structural
fixed thresholds
S2 policy
oracle granularity
```

---

# 7. COMPOSITIONAL ACTIVATION

The K10 result reportedly showed seven of nine mechanisms were inert.

The new system must have no decorative mechanism.

For every mechanism declare:

```text
input conditions
activation condition
state changed
downstream consumer
cost
positive fixture
null fixture
ablation
interaction partners
```

Required mechanisms:

```text
micro-association
plastic relation update
precision change
topology change
shadow field
procedure compilation
self-model allocation
world-model update
memory consolidation
```

Admission rule:

```text
standalone activity:
    required

known-positive causal effect:
    required

integrated activation frequency:
    nonzero where the workload calls for it

integrated ablation effect:
    nonzero or precisely licensed as unnecessary
```

Do not require every mechanism on every task.

Build a conditional material scheduler that activates only the cheapest useful mechanism.

The scheduler itself must be compared with simple fixed policies.

---

# 8. NATIVE PRECISION, CORRECTLY TESTED

P4 cannot be rerun using resource envelopes that never bind.

Use calibrated relative envelopes.

First measure the full reference footprint.

Then choose envelopes such as:

```text
2%
5%
10%
20%
40%
70%
100%
150%
```

of measured reference demand, or scale the developmental material until these budgets bind.

An envelope is admitted only when:

```text
memory pressure changes
at least one candidate changes behaviour
at least one nontrivial capability degrades or reallocates
the budget is enforced rather than descriptive
```

If an envelope does not bind, classify it as `nonbinding` and exclude it from the capability-density claim.

Test:

```text
full precision
post-hoc compression
native ternary
native quinary
exact microstore plus low-bit field
learned codebook
adaptive mixed-radix
```

Primary frontier measures:

```text
absolute utility
utility per resident byte
retained history per byte
learning per written byte
rare-case accuracy
calibration
recovery
latency
```

Do not select the smallest artifact when absolute capability is materially worse.

---

# 9. CANDIDATE TOURNAMENT

Build a bounded family.

```text
L0:
    selected prior field

L1:
    S2-derived associative monolith

L2:
    associative monolithic plastic field

L3:
    associative graph-plastic field

L4:
    associative cellular field

L5:
    associative state-space field

L6:
    associative event-sourced field

L7:
    exact-microstore plus mixed-radix structural field

L8:
    consolidation-first field

L9:
    minimal sufficient field

L10:
    Grok-original compositional field

L11:
    integrated winner
```

All candidates share the same external contracts.

Prefer the simplest candidate when performance is equivalent.

Do not require modularity.

Do not require tensors.

Do not require an LLM.

Do not require low precision in components where exact representation is more efficient or reliable.

---

# 10. DEVELOPMENTAL CHALLENGES

Freeze architecture and update laws before revealing principal structures.

Challenge families must include:

```text
new associations requiring fast learning
new concepts requiring consolidation
exceptions requiring concept split
redundancy requiring merge
new causal systems
new sensor mappings
new tools
new model competence
long unresolved goals
negative transfer traps
multimodal composition
resource pressure
```

A single developmental history should move through:

```text
micro-association
→ repeated evidence
→ consolidation
→ structural transfer
→ exception
→ revision
→ compiled procedure
→ later reuse
```

Test whether the architecture changes future thought rather than only retrieving stored answers.

---

# 11. GROK PROGRAM

Use Grok heavily but efficiently.

Target:

```text
minimum distinct roles:
    40

preferred:
    56–72
```

Required cells:

```text
representation-versus-architecture
associative-memory design
low-bit arithmetic
mixed-radix learning
rewrite granularity
plasticity
metaplasticity
topology
mechanism composition
conditional scheduling
capability-density instrumentation
S2 fairness
challenge generation
counterfeit detection
statistics
performance
continuity
checkpoint security
publication
```

Rounds:

```text
blind diagnosis
candidate proposals
cross-examination
challenge commitment
code review
post-canary review
post-pilot review
final hostile review
```

Use independent Grok cells to attempt each challenge with the strongest baseline.

Do not use Grok judgments as endpoints.

Record every invocation and disposition.

---

# 12. EXECUTION OPTIMIZATION

The latest run established useful operational lessons. Make them policy.

## 12.1 Protect sequential continuity

The continuity lane receives higher process priority than campaign and backfill workers.

Suggested relative policy on macOS:

```text
continuity:
    nice 5 or better available non-root priority

campaign and backfill:
    nice 19
```

Continuously measure the lane's cumulative CPU time.

A live PID with flat CPU time is a wedge.

## 12.2 Adaptive worker count

Benchmark safe worker counts.

Maintain:

```text
host idle target:
    approximately 0–5%

continuity CPU target:
    enough to preserve declared cycle and event requirements
```

Reduce workers when the continuity lane loses required throughput.

Increase workers when idle resources remain.

## 12.3 Heavy-first backfill

Queue independent long jobs first:

```text
sensitivity
binding envelopes
candidate ablations
additional replication
clean clone
```

Run clean clone concurrently when it does not contend materially.

## 12.4 Multiprocessing safety

Every executable module must use:

```python
if __name__ == "__main__":
    main()
```

Avoid nested uncontrolled process pools.

Run a spawn-safety canary before large campaigns.

## 12.5 Resume and collection

Use content-addressed receipts and checkpoints.

Collectors must refuse when:

```text
campaign log incomplete
required artifact missing
checkpoint does not restore
raw recomputation differs
mutation survives
source digest differs
```

Never hand-write a missing receipt.

## 12.6 No arbitrary duration

Long continuity is mechanism-driven.

If the selected field uses genuine elapsed-time dynamics, run at least 12 hours.

If it has no real time-dependent mechanism, do not use wall time as a proxy. Freeze cycle, event, interruption, migration, checkpoint, and developmental-history requirements instead.

Any duration change must be preregistered before the decisive lane starts.

## 12.7 Parallel acquisition

Use resumable parallel downloads, checksum verification, extraction, preprocessing, and caching by digest.

Do not acquire models or corpora that do not answer a defined question.

---

# 13. CHEAP CANARIES

At minimum:

```text
C01 exact micro-writes improve a held-out result
C02 low-bit micro-writes improve a held-out result
C03 micro-write reversal removes benefit
C04 repeated associations consolidate
C05 consolidation reduces future cost
C06 consolidated structure transfers
C07 exception reopens and revises structure
C08 granularity allocator chooses micro over topology
C09 topology is chosen when micro-writes are insufficient
C10 each integrated mechanism activates
C11 each necessary mechanism has an integrated ablation effect
C12 exact field closes or fails to close the S2 gap
C13 low-bit S2 isolates representation cost
C14 binding envelopes actually bind
C15 adaptive precision changes behaviour under pressure
C16 wrong-history controls remain clean
C17 shadow fields preserve authoritative state
C18 procedure failure returns to flexible reasoning
C19 checkpoint restore reproduces developed state
C20 process replacement preserves goals
C21 mutation detectors reject decorative mechanisms
C22 activation remains false
```

Cheap results admit mechanisms. They do not earn the terminal claim.

---

# 14. MODERATE PILOT

Use:

```text
48–96 histories
all surviving candidates
12–16 challenge families
250,000–1,000,000 episodes
```

Estimate:

```text
headroom
variance
power
write economy
resource frontier
mechanism activity
continuity cost
runtime
memory
```

Require a post-pilot Grok review.

Eliminate candidates that:

```text
lose decisively
depend on decorative mechanisms
fail binding budgets
have invalid controls
cannot restore
```

---

# 15. FINAL FREEZE AND CAMPAIGN

Freeze:

```text
source
architecture
update laws
precision rules
budgets
baselines
challenge generators
splits
seeds
statistics
mutations
claim boundary
```

Principal scale is chosen from pilot power.

Preferred bounds:

```text
128–256 histories
5,000–30,000 principal units
replication at least one third of principal
hidden composition at least one third
1–5 million episodes
```

Run independent lanes in parallel.

Run a real 12-hour continuity lane only when continuous-time mechanisms are active and scientifically relevant.

Otherwise run the frozen event/cycle-based continuity authority.

---

# 16. PRIMARY CLAIMS

```text
P1:
    fine-grained associative plasticity matches or exceeds S2 write efficiency

P2:
    structural consolidation improves transfer and lowers repeated cost

P3:
    conditional granularity selection beats fixed update strategies

P4:
    integrated mechanisms have causal nondecorative value

P5:
    native precision improves a binding capability-resource frontier

P6:
    developmental histories create useful future organization

P7:
    the selected material survives interruption, migration, and model replacement

P8:
    multimodal information updates one coherent field

P9:
    the selected field beats the strongest equally plastic monolithic alternative

P10:
    the advantage replicates and survives hidden task composition
```

Outcome A requires:

```text
P9 and P10 effect >= 0.05
95% CI lower bound > 0
oracle headroom > 0.05
replication positive
hidden composition positive
zero mutation survivors
clean clone passing
```

---

# 17. MUTATIONS

Add mutations for:

```text
associative store reads target answers
write cost is undercounted
candidate receives more write bandwidth
S2 is artificially precision-limited
nonbinding envelope reported as binding
mechanism registered but never consumed
ablation bypassed through alias
topology stores answers instead of structure
consolidation copies outputs
precision promotion reads outcomes
procedure loses accuracy
shadow field reads future authoritative state
checkpoint omits microstore
checkpoint omits scheduler
collector accepts unanchored receipts
multiprocessing child recursively launches main
activation becomes true
```

Every detector needs an injected defect and paired clean case.

Require zero survivors.

---

# 18. PUBLICATION

Use:

```text
branch:
    agent/substrate-cognitive-material-genesis-ii

ready tag:
    substrate-cognitive-material-ii-ready

terminal tag:
    substrate-cognitive-material-ii-terminal
```

Publish:

```text
prior-result reconstruction
representation-versus-architecture diagnosis
write-economy ledger
candidate tournament
mechanism-activity matrix
binding resource frontiers
developmental curricula
continuity
principal
replication
hidden composition
Grok archive
mutations
clean clone
limitations
tangible-sandbox handoff
```

Return only after one terminal outcome is merged and tagged.

The thesis is:

> A viable cognitive material must learn cheaply at the associative scale, reorganize usefully at the structural scale, and invoke expensive topology only when simpler change is insufficient.
