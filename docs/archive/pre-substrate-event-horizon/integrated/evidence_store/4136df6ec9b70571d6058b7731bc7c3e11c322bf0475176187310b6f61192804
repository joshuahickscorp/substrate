# Temporal core start authority

Inherits `agent/mop-experimental-method-reformation` at `5e4ce5ed67b2`, tag `mop-method-terminal` resolving to commit
`5b69a6b47452`, draft PR #34. The remote head matches the local head. Two commits sit
between the tag and the branch head; both are custody and verification only, and the reason this program
branches from the head rather than the tag is that the first of them tracks the raw shard receipts.

## Bound method result

Validity kernel 3103 lines,
12 of
17 admission stages before principal compute,
12 calibration worlds all passing,
20 defect mutations all rejected, statement coverage
98.9 percent, branch coverage
94.4 percent, 0 confirmed
open defects. Evidence fabric root `c59e4573cef0272fbba6fd17`.

## Bound E1 result

{
 "har_stream": {
  "at_linear": 0.455,
  "at_mlp": 0.45525,
  "group_lower_95_cb": 0.38605
 },
 "speech_stream": {
  "at_linear": 0.46475,
  "at_mlp": 0.49075,
  "group_lower_95_cb": 0.41545
 }
}

a trainable temporal core and persistent state produce substantial value on two strongly temporal controlled beds, and increasing readout capacity does not explain the effect.

Forbidden: complete substrate positive, cross domain substrate positive, continual learning positive, plasticity controller positive, architecture selection, external replication, activation.

## Bound E4 result

fast owned state can alter behaviour without parameter change, and the tested state only adaptation rule is not competitive. the E4 state only rule is not selected and is not reused unchanged.

## Objective

Explain the core before building around it. Separate recurrence, history, capacity, horizon, readout and
optimization, on more than one valid bed, with more than one implementation, and only then choose the
smallest core that preserves the evidence.
