# MOP Substrate Genesis II Ledger

Program `mop-owned-substrate-genesis-v0` on `agent/mop-substrate-genesis-v2` off `06860b2`.

Premise: an owned multi-timescale latent system may beat conventional single-timescale/stateless learners on cost-adjusted moldability, starting from strong SIMPLE plasticity. Must beat strong matched controls after full cost accounting. A tie is a null. >=5 stable seeds (C1 lesson). Activation false.

## Waves: 1 authority DONE; 2 implementation ACTIVE; 3 battery; 4 domain1; 5 domain2; 6 transfer; 7 plasticity gate; 8 verification.

## Binding null constraints: no learned replay selector / retriever / simulation allocator / plasticity controller by default (all G2/G3 nulls); established replay (GDumb/reservoir) is the active baseline; failed mechanisms are frozen controls only.

Resume: read MOP_SUBSTRATE_GENESIS_STATE.json; continue each checklist item from next_exact_action.
