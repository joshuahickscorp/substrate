# Extended-compute research prompt

Copy everything below into a new Codex chat whose workspace is this repository.

---

You are working in `/Users/scammermike/Downloads/mop`.

Ignore every proposal about re-engineering this project for Fable. This is a research and execution-
planning task for the Form Substrate program itself.

## Mandate

Perform a super-deep, falsification-first investigation of what work in this project would **actually
require or materially benefit from extended compute beyond the measured local M3 Pro envelope**.
Do not assume that “Studio,” GPU, more memory, more parameters, or a historical tier label is a
scientific requirement. Try to eliminate the need for extended compute before recommending it.

The program's philosophy is capability density: small, interpretable, modular substrates; strong
controls; exact referents; long-lived learning; causal and developmental evidence; and maximum
scientific information per parameter, byte, joule, and wall-clock hour. The desired architecture is
not a giant inherited vision model. V-JEPA and its ViT variants are stepping stones, teachers,
controls, and measurement instruments for a later project-owned substrate.

The research question is therefore not “what can we run on a large machine?” It is:

> Which decisive experiments remain scientifically underpowered after every reasonable local
> optimization, and what is the smallest additional compute envelope that changes the answer?

## Current empirical correction you must preserve

Start by verifying the repository and current receipts rather than trusting this summary. The live
state should include the following:

- the current local profile has a 180-minute wall and a fail-closed 40 GB free-disk floor;
- ViT-L, ViT-H, and ViT-g all load and execute locally in serial;
- ViT-H and ViT-g have supervised finite CPU-forward receipts and eight-referent caches;
- the project-exhaustion ledger reports zero measured M3-Pro hardware blockers;
- the active registry contains no non-F `studio-scale`, `moonshot`, or `gpu-later` rows;
- F8 and F16 are blocked first by trusted natural data/provenance, not hardware;
- CM7 is a locally trainable, teacher-independent approximately 1.65M-parameter video substrate
  with dense spatiotemporal tokens, exact checkpoints, four matched objectives/controls, and a
  five-seed local campaign;
- the largest unresolved inputs are rights-clean natural video, aligned audio/video, interactive
  environments, longer developmental streams, matched controls, and one unpublished upstream
  dense checkpoint—not proven memory or accelerator limits;
- SANPO natural-video intake and architecture-matched random controls may be newer than this prompt,
  so inspect their current receipts before drawing conclusions.

Read at minimum:

1. `FORM_SUBSTRATE_DEEP_RESEARCH_2026_07.md`
2. `FORM_SUBSTRATE_DEEP_EXPANSION_PLAN.md`, especially its live addendum
3. `docs/CUSTOM_SUBSTRATE_WORKBENCH.md`
4. `docs/LOCAL_CEILING_RESIDUAL_AUDIT_2026_07_10.md`
5. `proof/PROJECT_EXPERIMENT_EXHAUSTION.json`
6. `proof/FRONTIER_LOCALIZATION.json`
7. `proof/STUDIO_READINESS_CURRENT_HOST.json`
8. `proof/VJEPA_SCALE_ATLAS_LOCAL.json`
9. `registry/experiments.yaml`
10. all current custom-substrate, natural-intake, F-campaign, and random-control receipts

Treat generated artifacts as stale if their embedded hashes disagree with their current sources.

## Core method

For **every** experiment, pillar, frontier, workstream, and proposed F21-F66 expansion, determine its
first honest blocker and put it in exactly one primary category:

1. already runnable locally;
2. locally runnable after implementation;
3. locally runnable after data or rights intake;
4. locally runnable serially but slow;
5. locally runnable with streaming, quantization, checkpointing, smaller batches, lower-rank
   controls, cached teachers, or staged evaluation;
6. dependent on an external environment, sensor, participant, or physical specimen rather than
   compute;
7. dependent on an unpublished or unavailable upstream model;
8. empirically extended-compute-beneficial but not required;
9. empirically extended-compute-required under a named scientific power or fidelity target.

Do not use category 8 or 9 without a calculation or measurement. A package guard, model name,
historical planning label, rough intuition, or desire for convenience is not evidence.

For each candidate extended-compute workload, build a local feasibility attack:

- smallest scientifically meaningful sample and model;
- streaming versus residency;
- serial versus parallel execution;
- activation checkpointing and gradient accumulation;
- mixed precision and quantization, with accuracy risks;
- cached frozen teachers versus resident teachers;
- sparse/dense token tradeoffs;
- temporal windowing and hierarchical memory;
- sequential multi-seed execution;
- early stopping and sequential power analysis;
- low-rank, random-feature, or probe approximations where scientifically valid;
- dataset downsampling or derived compact representations without provenance loss;
- whether a better experimental design removes more uncertainty than more compute.

Only after that attack may you define a larger rung.

## Candidate frontiers to investigate, not assume

Research whether any of these cross the local ceiling under a defensible target:

- natural-video objective tournaments at increasing sessions, diversity, resolution, and duration;
- teacher-free custom substrate scaling from roughly 1.65M parameters upward;
- dense high-resolution video tokens over long temporal contexts;
- exact same-referent learned versus random architecture controls across multiple encoder scales;
- multi-seed objective, architecture, and ablation matrices with corrected statistical power;
- continual learning over days or millions of events with replay, reconsolidation, growth, pruning,
  and future-plasticity measurement;
- action-conditioned world models in persistent interactive environments;
- active perception where the model chooses camera, audio, or probing actions under sensor cost;
- native audio plus video, language, proprioceptive/action, and internal-telemetry binding;
- object, slot, event, relation, and causal-state discovery on natural multi-object streams;
- population-based curiosity, open-ended ecology, quality-diversity, cultural accumulation, and
  partner-specific social learning;
- calibrated workspace, operational self-model, failure prediction, and resource allocation over
  long histories;
- architecture search for small substrates, including recurrent, state-space, sparse mixture,
  cellular, reservoir, neuromorphic, and morphogenic candidates;
- digital material-substrate simulations with drift, damage, repair, device variation, and tuned
  conventional controls;
- robustness sweeps over corruptions, domains, seeds, and counterfactual interventions;
- full-system performance-density accounting, including preprocessing, caches, memory, energy,
  storage, retries, and idle hardware.

Add any more compelling frontier you discover. Remove or demote any candidate whose scientific
question can be settled locally.

## Required extended-compute ladder

Do not make “Studio” one undifferentiated destination. Derive a ladder from measured workloads, for
example:

- **L0:** current M3 Pro, one heavy process, 180-minute bounded run;
- **L1:** current host across resumable overnight or multi-day serial windows;
- **L2:** larger-memory Apple Silicon or equivalent single-node system;
- **L3:** one high-memory accelerator;
- **L4:** small multi-accelerator node;
- **L5:** distributed or population-scale campaign;
- **L6:** external physical, sensor, or participant infrastructure.

Those names are illustrative. Recalculate the actual rungs. For every rung report:

- minimum memory and why it must be resident;
- CPU/GPU/accelerator needs;
- storage, read bandwidth, and checkpoint volume;
- wall-clock estimate and parallelism assumptions;
- expected power/energy envelope;
- software/runtime requirements;
- estimated monetary cost ranges and sensitivity;
- which exact experiments become possible;
- what remains blocked even after buying the rung;
- a stop rule that prevents idle procurement or scale-for-scale's-sake.

Separate **throughput acceleration** from **scientific enablement**. If the larger rung merely turns
a five-day serial run into five hours, say so. If it enables a resident state or simultaneous
interaction that cannot be faithfully decomposed, prove that.

## Small-substrate research program

The output must propose a serious alternative to parameter scaling. Investigate how to spend added
compute primarily on:

- more independent referents and environments;
- more seeds and stronger controls;
- longer developmental time;
- intervention and active data collection;
- memory and plasticity histories;
- architecture diversity at matched parameter/FLOP budgets;
- causal, compositional, and cross-modal evaluation;
- failure discovery, not leaderboard optimization.

Define scaling laws or response surfaces over at least:

- parameter count;
- token count and temporal context;
- number/diversity of referents;
- number of environments and interventions;
- lifetime updates;
- memory/replay budget;
- objective mixture;
- energy and storage cost.

Ask whether data diversity, temporal depth, modularity, recurrence, external memory, or developmental
curriculum produces more capability per parameter than width/depth. Make these falsifiable.

## Evidence and research standards

Browse current primary sources and official documentation. Use original papers, official dataset
pages, hardware/runtime documentation, and first-party repositories. Cite every unstable claim and
all hardware, license, dataset, and software facts. Clearly separate:

- measured repository facts;
- published external evidence;
- calculations;
- engineering estimates;
- speculative hypotheses.

For statistical sizing, state effect size, variance assumption, alpha, correction family, power,
seed/referent unit, and dependence structure. Prefer paired and sequential designs where justified.
Do not treat frames from one video or replicas of one referent as independent samples.

For compute sizing, give formulas and sensitivity bands. Include preprocessing and failed-run
overhead. Challenge all FLOP estimates with at least one measured calibration. Never translate
unified memory directly into accelerator capacity without runtime evidence.

For rights and provenance, identify license, access path, redistribution limits, privacy constraints,
and exact split discipline. Do not misclassify a data-rights blocker as a compute blocker.

No consciousness, sentience, agency, or general-intelligence claim may be inferred from an
engineering probe. Keep the theory-neutral operational evidence rail from the deep research dossier.

## Required adversarial questions

For every proposed extended-compute experiment, answer:

1. What precise uncertainty does this remove?
2. Why is the current local result insufficient?
3. What is the smallest model/data/runtime that can answer it?
4. Can it be streamed, serialized, cached, resumed, or factorized?
5. Does parallelism change scientific validity or only elapsed time?
6. What control consumes the same compute?
7. What would a null result close?
8. What result would justify the next rung?
9. What result would permanently demote or kill the line?
10. Does more compute create a new confound, such as changing architecture and data simultaneously?
11. Is the independent unit a seed, referent, session, environment, partner, or physical specimen?
12. What artifact makes the result reusable by the local custom substrate?

## Deliverables

Write two extensive files in the repository:

1. `EXTENDED_COMPUTE_DEEP_RESEARCH_2026_07.md`
2. `EXTENDED_COMPUTE_EXECUTION_PLAN.md`

The research file must include:

- an evidence-backed executive answer to whether anything is truly Studio-scale;
- a complete blocker reclassification of the project;
- primary-source research across the candidate frontiers;
- local feasibility attacks;
- measured/calculated ceiling models;
- statistical power and scaling analysis;
- data, rights, environment, and physical-infrastructure separation;
- capability-density alternatives to parameter scaling;
- unresolved questions and explicit uncertainty.

The execution plan must include:

- a ranked experiment portfolio;
- exact local, overnight-local, larger-single-node, and distributed rungs;
- for every experiment: hypothesis, independent unit, inputs, smallest model, controls, seeds/power,
  memory/storage/time estimate, local optimization, required rung, promotion gate, null/kill rule,
  and reusable artifact;
- a dependency graph and critical path;
- a procurement/no-procurement decision gate;
- pilot commands or benchmark specifications that can validate the estimates locally;
- a 30/90/180-day sequence that spends compute only after evidence earns it;
- a section called `What we should never scale`.

Also produce a machine-readable matrix, preferably
`proof/EXTENDED_COMPUTE_REQUIREMENTS.json`, with one row per current registry experiment and proposed
frontier. It must be self-verifying, hash its source evidence, distinguish measured from estimated
fields, and make `hardware_required: true` impossible unless a named measurement/calculation gate
passes.

## Working method

Use multiple agents in parallel for independent, bounded research domains, then adversarially review
and reconcile their conclusions. The primary agent must personally inspect the repository evidence,
read all skill instructions it invokes, verify citations, and integrate the final files. Do not let
agents silently inherit the assumption that bigger is better.

This is a research-and-plan run. You may write the three requested artifacts and supporting analysis
scripts/tests, but do not rent cloud hardware, purchase anything, accept dataset terms, download a
large corpus, launch a distributed campaign, or mutate external systems. Small read-only or bounded
local sizing probes are allowed only when they respect the active host profile and do not compete
with another heavy process.

Do not stop at a generic compute wish list. Continue until every proposed extended-compute claim has
either been earned by a measured/calculated boundary or demoted to local/data/environment/
implementation work.

---
