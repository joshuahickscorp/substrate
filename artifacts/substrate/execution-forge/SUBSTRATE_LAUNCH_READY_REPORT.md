# Substrate launch-ready report

Substrate is publication-complete and certified at the launch boundary. The scientific verdict remains
`certified_cognitive_scaffold`. The passed gates remain `grounded_closed_loop`,
`unity_under_conflict`, and `world_self_control_value`. The mechanism nulls remain
`endogenous_allocation`, `cross_domain_continuity`, and `procedural_transfer`. Activation remains
`false`.

## What the 19 units are

The executor is a **terminal deterministic synthesis**, not a long scientific experiment. It contains
zero new scientific work units and performs zero new trials. It verifies instruments, deterministically
recomputes already admitted measurements, rejects mutations, regenerates artifacts, synthesizes reports,
and seals existing results. No substantive frozen work is missing.

The complete depth classification is in `SUBSTRATE_LONG_RUN_WORK_LEDGER.json`.

## Selected execution

- One supervisor
- One persistent worker
- One native thread
- Subprocess reference median: 8.923 seconds
- Selected persistent-worker median: 7.112 seconds
- Speedup: 1.255x
- Selected measured peak RSS: 316.2 MiB
- Terminal range: 6.8 to 7.3 seconds

Two conservative workers measured 6.574 seconds, only 7.6 percent better than the proper persistent
baseline. That is below the declared 15 percent concurrency threshold. Four and eight workers raised
memory and variance. The smallest safe resource cost therefore wins.

The final parity rerun matched all 44 normalized artifacts between clean subprocess and persistent-worker
execution. No Rust conversion is justified.

## Verification

- 231 active tests passed
- 45 independent checks agreed, with 0 disagreements and 0 broken seals
- 32 of 32 mutations were rejected, with 0 survivors
- 29 destructive rehearsal checks passed
- 13 of 13 historical objects passed SHA-256 verification
- GitHub CI passed on the authoritative branch, PR head, and merged `main`
- A fresh clone from the GitHub remote passed clean non-editable installation, tests, verification,
  mutation, artifact-drift, and activation checks
- Status remains 0 of 19 completed, with no live worker, claim, stop switch, or launch receipt

## Extinction and publication

All active legacy product surfaces are gone. Seven launch agents were unloaded and moved intact to a
recoverable Trash folder. Eight old branch names and 285 old tag names were deleted after reachability
checks. Two unique commits from one obsolete branch were preserved at
`archive/substrate-extreme-condensation-unmerged` before its old name was removed.

PR #35 merged at `4992cc174d4490b8418960056dca07ae9e6e9e79`. The repository default is `main`; its description and
topics identify Substrate. The old repository name redirects to this repository and no separate active
repository exists.

The terminal tag is `substrate-v1-launch-ready`.

## Operator boundary

```bash
substrate run
substrate status
substrate stop
substrate resume
```

The exact launch command is `substrate run`. It has not been executed.
