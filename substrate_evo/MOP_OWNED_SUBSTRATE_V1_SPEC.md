# Owned Substrate v1 Spec

Implemented + tested on a valid temporal bed (HAR raw, order-headroom confirmed); NO candidate selected (Substrate Event Horizon). Owned: temporal projection, fast GRU state, medium EMA context, slow workspace, GDumb memory, heads. Five architectures across five premises all null vs converged temporal baselines. Continual headroom is memory-capacity-bound (+0.284 unbounded vs GDumb), and better bounded memory is closed (R1/P1R). Activation false.
