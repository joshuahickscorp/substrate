# MOP Substrate Genesis II: Start Authority

Append-only. Base commit 06860b2. Gen2 (89eeca5) and Gen3 (06860b2) immutable. Collapse branch untouched.

## Frozen Gen3 interpretation
- C1 (P1R sampling priority): terminal invalid_bed. Stable oracle headroom over uniform GDumb +0.010 (< 0.020 threshold); the +0.169 was a two-seed artifact; learned priority 0.18-0.19, harmful. Closed; frozen negative control only.
- C3 (model-error-aware simulation): precompute gate failure, no canary. Even an oracle error-aware allocator did not beat reactive/simple planning. Closed; no learned simulation allocator.

## Boundary
No mechanism has external confirmation of incremental value over strong established alternatives. Substrate Genesis tests an OWNED multi-timescale architecture with strong SIMPLE plasticity, before any new learned meta-controller.

## Premise
An owned multi-timescale latent system may beat conventional single-timescale/stateless learners on cost-adjusted moldability. It must beat strong matched controls after full cost accounting; complexity is not credited as intelligence. A tie is a null.
