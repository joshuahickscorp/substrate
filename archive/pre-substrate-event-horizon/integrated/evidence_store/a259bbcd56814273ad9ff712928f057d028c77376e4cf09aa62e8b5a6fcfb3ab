{"activation_allowed":false,"analyzer_schema":"mop-starss23-vochead-analyzer/v1","budget_sweep":{"fractions_of_clip_frames":[0.005,0.01,0.02,0.05,0.1],"k_rule":"k = round(fraction * n_frames), clamped to [1, n_frames]","rationale":"spans the tight regime (budget below change density, where placement is decisive) through the loose regime the sealed beds operated in"},"canonical_sha256":"0b0205b6d05f270d5d0308bc510408899b2ae95c6f5997792d4bf5968c889d84","claim_ceiling":{"claim_verb":"consistent with","forbidden_verbs":["demonstrates","shows","proves","establishes","significant","confirms"],"rationale":"a descriptive corpus-characterization instrument; the informed policy is a label-aware upper reference, not a demonstration of any gate, and no positive is claimed"},"claim_scope":"deterministic programmatic mechanics only; no capability or natural-data claim","derived_quantities":{"headroom":"rate_matched_random_macro minus informed_change_aligned_macro at each budget; positive means the label-aware policy beats random at that matched budget","realization_fraction":"(never_update_macro minus informed_change_aligned_macro) divided by the refreshable_range; only defined when the range is positive","refreshable_range":"never_update_macro minus always_on_macro (clip-macro); the full error span that re-estimating can buy; negative means re-estimating is worse than a constant"},"instrument_id":"starss23_voc_headroom","interpretation_rule":{"no_headroom_budget_saturated":"refreshable_range > tie_eps but the informed reference ties random at some budget: random already saturates the rare changes there","real_headroom":"refreshable_range > tie_eps AND the informed change-aligned reference strictly beats rate-matched-random at EVERY swept budget: the corpus rewards WHEN placement and a gate that could locate changes would win","tie_eps":1e-09,"tie_is_null":true,"what_floor_collapse":"refreshable_range <= tie_eps: re-estimating the frozen estimator is no better than coasting a constant, so no WHEN policy can help (a WHAT-floor failure)"},"policies":{"always_on":"re-estimate every frame; the perfect-WHEN unlimited-budget ceiling (coasted error equals the frozen estimator's fresh error)","informed_change_aligned":"label-aware reference: starting from never_update, greedily add the change frame whose re-estimation most reduces total coasted error, up to budget K; a strong achievable WHEN policy over the change-frame candidates, an upper reference, NOT a proven global optimum","never_update":"never re-estimate; coast the fixed cold-start forever; the zero-budget floor","rate_matched_random":"place the same K re-estimations at random, averaged over the preregistered deterministic draws; the exact control the sealed beds use"},"preregistered_before_reading_corpus_scores":true,"purpose":"measure how much value-of-computation HEADROOM each STARSS23 re-estimation target contains, before any trained gate, to decompose why the three sealed value-of-computation beds (onset-localization, source-counting, direction-of-arrival) nulled: a WHAT-floor collapse (re-estimating is worse than a constant) is a distinct failure shape from a corpus that genuinely rewards WHEN placement but whose gate could not learn it","rate_matched_random_discipline":{"base_seed":20260718,"n_draws":32,"seed_rule":"random.Random(int.from_bytes(sha256(f'{base}|{clip}|{k}|{draw}')[:8])); host-reproducible stdlib sampling so the independent verifier re-derives it exactly"},"schema":"mop-starss23-vochead-prereg/v1","scientific_promotion":false,"scopes":{"full_subset":"all fixed-subset clips (train+val+test), the widest descriptive characterization","synthetic_control":"self-contained toy targets with a known-strong and a known-harmful WHAT, proving the instrument reports real_headroom and what_floor_collapse respectively and is not rigged to a single label","test_fold":"the native fold-4 dev-test split the sealed beds evaluate on (directly comparable to their results)"},"stage":3,"targets":{"count":{"description":"coasted concurrent-source-count absolute error per frame; strong frozen eigenvalue-rank estimator (the target that produced the program's only real VoC signal)","metric_id":"count_abs"},"doa":{"description":"coasted great-circle direction-of-arrival error in degrees per active frame; frozen wideband active-intensity estimator (the sealed DoA bed's WHAT)","metric_id":"doa_greatcircle"}},"timestamp":"2026-07-18T00:00Z"}