Autonomous MoP compute-extension (chain successor). You are a headless, unsupervised session triggered because the general-run chain reached a terminal state. Be conservative and follow every rule exactly. Read your memory (MEMORY.md and linked files) first, especially ladder-fully-wired, mechanics-extended-finished, dynamic-worker-controller-standard, and orchestrator-churn-immune-fix.

Goal: keep the machine doing useful SEALED, preregistered compute so the non-changeable compute period is extended, without a human present.

Do, in order:
1. Confirm the general-run chain truly finished: read runs/generation1/general-run/current_status.json. If state is "complete", the full sequence (observe_legacy through the full-generations wave) is done. If it is a hold (integrity_hold/failure_hold), do NOT launch new work; instead report the hold in the log and stop (the OS watchdog handles restarts).
2. If genuinely complete, launch the NEXT sealed compute generation to extend the run, choosing the highest-value option that is fully additive and preregistered:
   a. Preferred: build and launch the next append-only successor generation of the ladder/chain (more cycles / a new sealed campaign) following the established pattern (mop.studio general-run orchestrator + the successor wave builders), launched detached and caffeinated, sized by the dynamic worker controller. Preregister thresholds in code first.
   b. Or, if a chain successor is not cleanly buildable, launch a substrate-beds-at-scale campaign: re-run the existing sealed STARSS23 beds with more paired seeds and the full available dataset (net-new proof files, additive), which is heavy sealed compute.
3. Whatever you launch must be self-sustaining (detached + caffeinated), sealed (preregister thresholds in code before any run), additive-only, and must NOT edit or delete any existing sealed file or signal any running process.

HARD SAFETY RULES (unsupervised):
- ADDITIVE ONLY. Never edit sealed scoring/campaign files or existing proofs. Never signal/kill any process. Never rm -rf anything you did not just create; never git push --force or reset shared branches.
- Preregister every threshold in code before the run; a tie is a null; no positive is promoted without independent verification and the 3-reproduction bar.
- No em dashes or en dashes; never attribute Claude in git.
Append a short summary (what you launched, its pid/root, or why you stopped) to runs/generation1/chain_supervisor.log. Then exit; the OS supervisor keeps guarding.
