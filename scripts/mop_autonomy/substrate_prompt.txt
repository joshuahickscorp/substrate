Autonomous MoP substrate advancement (STARSS23 ESCS bed). You are a headless, unsupervised session; be conservative and follow every rule exactly. Read your memory (MEMORY.md and the linked files) first for context.

Context: the onset-localization value-of-computation question is thoroughly walled with 7 sealed nulls (4 gate-firing-policy variants + 3 frozen featurizers; see memory starss23-onset-localization-wall). The next Stage-3 move is a DIFFERENT value-of-computation question on the same rights-clean STARSS23 FOA data.

PRIORITY ORDER for this run (in an ISOLATED git worktree off origin/main):
1. REPRODUCE A FRESH SIGNAL FIRST, but do NOT p-hack a resolved one. Check proof/ for any bed with verdict "mechanics-ok" and independent_scientific_confirmation false that has NO reproduction cohort yet (no proof/*<name>*REPRO*.json and no reproduction doc). If such a fresh signal exists, run its FIRST few bias-independent reproductions (vary data-split, featurizer+estimator, scoring-unit, gate-architecture). A reproduction survives only if the candidate strictly beats rate-matched-random in the SAME direction AND clears its preregistered SESOI AND the exact sign-flip floor. IMPORTANT: if a signal already HAS a completed reproduction cohort that concluded a mapped boundary (e.g. the source-counting signal: 2 of 4 survived, doc 28, below the >=3 bar), do NOT keep throwing new reproductions at it to reach 3 - that is p-hacking. Leave further probing of a resolved boundary as a human decision and move to step 2. Never auto-promote: a positive is FLAGGED for human review, never merged as a scientific confirmation.
2. ELSE OPEN THE NEXT QUESTION. If no signal needs reproduction, design and run ONE net-new sealed bed for the first not-yet-done of: (1) sound-source COUNTING, (2) direction-of-arrival ESTIMATION, (3) event CLASSIFICATION. If all are done, run one well-motivated variant of the most promising, or report the lane exhausted and recommend the next rights-clean dataset from docs/ESCS_DEEP_RESEARCH.md.

Follow the ESCS methodology (docs/ESCS_DEEP_RESEARCH.md) and mirror the existing bed pattern in src/mop/beds/starss23/: a frozen ZERO-trained-parameter featurizer; a single trained gate under 4096 params; matched ~6e10 lifecycle FLOP budget with C_train charged; THREE mandatory controls including rate-matched-random; a sealed referee appropriate to the task; 5 paired seeds; exact sign-flip; a compute-normalized SESOI PREREGISTERED IN CODE before any test score is read; a separately-authored independent verifier importing no producer code.

HARD SAFETY RULES (you are unsupervised):
- ADDITIVE ONLY. Create net-new files. NEVER edit a sealed scoring module or any existing sealed proof. NEVER edit files under runs/, and NEVER signal, kill, or disturb any running process (the census, the orchestrator, the watchdog): they run from the main working tree, which is why you work in a separate worktree off origin/main.
- No destructive shell (no rm -rf of anything you did not just create, no git push --force, no git reset --hard on shared branches, no branch deletion other than your own throwaway worktree branch).
- A tie is a null. NO positive enters any doc without an independent adversarial verification pass and it must be flagged for the 3-reproduction promotion bar, never auto-promoted. Unverified or faked = failure; a proven wall with a mechanism = success.
- No em dashes or en dashes anywhere; never attribute Claude in git (no Co-Authored-By, no Generated-with).

Verify with unit tests plus the independent verifier. If the result is a clean sealed NULL, commit and open a PR to main and merge it. If it is a POSITIVE, commit + open a PR but DO NOT merge; leave it open and clearly flagged for human review. Clean up your worktree. Append a 3-line summary (question, sealed verdict, PR link) to runs/generation1/substrate_research.log.
